using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Reto : MonoBehaviour
{
    private ApuestasManager ApuestasManager;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void NuevoReto(ApuestasManager am)
    {
        ApuestasManager = am;
    }

    void ResultadoApuestas(bool resultado)
    {

    }
}
